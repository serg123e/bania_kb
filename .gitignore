# первичные источники не публикуем (транскрипты чужих видео + yt-dlp дампы)
raw/
*.info.json

# служебное — не на публичный сайт
log.md
CLAUDE.md

# data/pages.json собирается в CI (workflow), не коммитим
data/

# мусор
.DS_Store
node_modules/
__pycache__/
*.pyc
